# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
  # default configuration for services in *this* file
  _defaults:
    autowire: true      # Automatically injects dependencies in your services.
    autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

#  _instanceof:
#    FC\Shared\Application\Command\CommandHandler:
#      tags: [{ name: messenger.message_handler, bus: command.bus }]
#
#    FC\Shared\Application\Query\QueryHandler:
#      tags: [{ name: messenger.message_handler, bus: query.bus }]
#
#    FC\Shared\Domain\Event\DomainEventSubscriber:
#      tags: [{ name: messenger.message_handler, bus: event.bus }]

  Psr\SimpleCache\CacheInterface: '@road_runner.key_value.psr16'
  Symfony\Component\Serializer\Normalizer\DenormalizerInterface: '@Symfony\Component\Serializer\SerializerInterface'

  # makes classes in src/ available to be used as services
  # this creates a service per class whose id is the fully-qualified class name
  FC\App\Controller\:
    resource: '../src/App/Controller/'
    exclude:
      - '../src/App/Controller/Api/api.php'
    tags: [ 'controller.service_arguments' ]

  FC\App\Command\GenerateKeyPairCommand:
    arguments:
      $secretKey: '%env(JWT_PRIVATE_KEY)%'
      $publicKey: '%env(JWT_PUBLIC_KEY)%'
      $passphrase: '%env(JWT_PASSPHRASE)%'
    tags:
      - { name: 'console.command', command: 'jwt:generate-keypair' }

  FC\App\Command\GenerateTokenCommand:
    tags:
      - { name: 'console.command', command: 'jwt:generate-token' }

  # add more service definitions when explicit configuration is needed
  # please note that last definitions always *replace* previous ones
